#include <stdio.h> 
#include "deposit.h"

int main() {
	int s,d,i;
	
	printf ("Vvedite summy vklada:");
	scanf ("%d",&s)
	printf ("Vvedite srok vklada:");
	scanf ("%d",&d); 
	
    if( i == 0)
      {
        s=itog(s,d);
      }
	else
	  printf ("Nekorektno vvedeni znacheniya!!");
}
